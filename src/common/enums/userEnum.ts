export enum ProviderEnum {
  system = 'system',
  google = 'google',
}

export enum RoleEnum {
  user = 'user',
  customer = 'customer',
  admin = 'admin',
  superAdmin = 'superAdmin',
  owner = 'owner',
  manager = 'manager',
}

export enum GenderEnum {
  male = 'male',
  female = 'female',
}
