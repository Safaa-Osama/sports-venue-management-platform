export enum BookingStatusEnum {
  pending = 'pending',
  confirmed = 'confirmed',
  cancelled = 'cancelled',
  completed = 'completed',
}

export enum PaymentStatusEnum {
  unpaid = 'unpaid',
  paid = 'paid',
  refunded = 'refunded',
}
